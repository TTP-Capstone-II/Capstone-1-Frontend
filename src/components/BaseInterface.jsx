import React, {useState} from "react";

const BaseInterface = () => {
    return (
        <form className="base-interface">
            <h2>Base Interface</h2>
            <p>This is a placeholder for the base interface component.</p>
            <p>It will render input fields from diff topic interfaces.</p>
        </form>
    );
}

export default BaseInterface;